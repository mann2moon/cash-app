const { Typography } = require('@mui/material');

const History = () => {
  return (
    <>
      <Typography variant="h3">History(Deposit && Withdraw)</Typography>
    </>
  );
};

export default History;
